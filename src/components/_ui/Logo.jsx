import React, { Fragment } from "react";
import logo from "../../images/logo.svg"

const Logo = () => (
    <Fragment>
        <div><img width={"140px"} src={logo} alt="Logo" /></div>
    </Fragment>
);

export default Logo;
